/// The display mode of an [InnovareSideMenu].
enum InnovareSideMenuMode {
  /// Full-width menu with icons, text, and section titles.
  expanded,

  /// Narrow rail showing only icons. Sub-items appear in popup overlays.
  collapsed,
}
