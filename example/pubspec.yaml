name: innovare_side_menu_example
description: Example app for innovare_side_menu package.
publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  innovare_side_menu:
    path: ../
  innovare_design: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  innovare_design:
    path: ../../innovare_design

flutter:
  uses-material-design: true
